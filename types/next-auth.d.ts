// next-auth.d.ts

import NextAuth, { DefaultSession, DefaultUser } from "next-auth";
import { JWT } from "next-auth/jwt";

// ──────────────────────────────────────────────────────────────
// ۱. تعریف و صادر کردن (export) تایپ CustomUser 
// ──────────────────────────────────────────────────────────────
export interface CustomUser extends DefaultUser {
  id: string; 
  accessToken?: string; 
}

// ──────────────────────────────────────────────────────────────
// ۲. گسترش NextAuth (اصلاح شده)
// ──────────────────────────────────────────────────────────────
declare module "next-auth" {
  /**
   * گسترش شیء Session
   */
  interface Session {
    user: {
      id: string;
    } & DefaultSession["user"];
    accessToken?: string; 
  }

  /**
   * 🔥 اصلاح: استفاده از Type Alias به جای interface خالی برای User
   * این کار اخطار "interface declaring no members" را رفع می‌کند.
   */
  type User = CustomUser; // 👈 تغییر از interface به type alias
}

// ──────────────────────────────────────────────────────────────
// ۳. گسترش JWT 
// ──────────────────────────────────────────────────────────────
declare module "next-auth/jwt" {
  /**
   * گسترش JWT
   */
  interface JWT {
    id: string;
    accessToken?: string;
  }
}